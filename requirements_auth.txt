# requirements-auth.txt
# Dépendances additionnelles pour le module auth
# À installer avec : pip install -r requirements-auth.txt

authlib>=1.3.0          # OAuth client (Starlette integration)
PyJWT[crypto]>=2.8.0    # JWT creation/verification + support ES256 (Apple)
SQLAlchemy>=2.0.0       # ORM pour la DB users
itsdangerous>=2.1.0     # Requis par Authlib/Starlette SessionMiddleware